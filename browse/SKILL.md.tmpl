---
名前：ブラウズ
プリアンブル層: 1
バージョン: 1.1.0
説明: |
  QA テストとサイトのドッグフーディングのための高速ヘッドレス ブラウザ。任意の URL に移動し、操作します
  要素、ページ状態の確認、アクションの前後の差分、注釈付きスクリーンショットの取得、チェック
  応答性の高いレイアウト、フォームとアップロードのテスト、ダイアログの処理、要素の状態のアサート。
  コマンドごとに最大 100 ミリ秒。機能のテスト、デプロイメントの検証、ドッグフードなどを行う必要がある場合に使用します。
  ユーザー フローを確認するか、証拠を添えてバグを報告してください。 「ブラウザで開く」、「テストする」ように求められた場合に使用します。
  サイト」、「スクリーンショットを撮る」、または「ドッグフード これ」。 (Gスタック)
許可されたツール:
  - バッシュ
  - 読む
  - ユーザーに質問する

---

{{前文}}



永続的なヘッドレス Chromium。最初に自動開始を呼び出し (~3 秒)、次にコマンドごとに ~100 ミリ秒を呼び出します。
状態は呼び出し間 (Cookie、タブ、ログイン セッション) に維持されます。

{{BROWSE_SETUP}}

## コア QA パターン

### 1. Verify a page loads correctly
```bash
$B goto https://yourapp.com
$B text                          # content loads?
$B console                       # JS errors?
$B network                       # failed requests?
$B is visible ".main-content"    # key elements present?
```

### 2. ユーザー フローをテストする
```bash
$B goto https://app.com/login
$B snapshot -i                   # see all interactive elements
$B fill @e3 "user@test.com"
$B fill @e4 "password"
$B click @e5                     # submit
$B snapshot -D                   # diff: what changed after submit?
$B is visible ".dashboard"       # success state present?
```

### 3. アクションが機能したことを確認する
```bash
$B snapshot                      # baseline
$B click @e3                     # do something
$B snapshot -D                   # unified diff shows exactly what changed
```

### 4. バグレポートの視覚的証拠
```bash
$B snapshot -i -a -o /tmp/annotated.png   # labeled screenshot
$B screenshot /tmp/bug.png                # plain screenshot
$B console                                # error log
```

### 5. すべてのクリック可能な要素を検索します (非 ARIA を含む)
```bash
$B snapshot -C                   # finds divs with cursor:pointer, onclick, tabindex
$B click @c1                     # interact with them
```

### 6. 要素の状態をアサートする
```bash
$B is visible ".modal"
$B is enabled "#submit-btn"
$B is disabled "#submit-btn"
$B is checked "#agree-checkbox"
$B is editable "#name-field"
$B is focused "#search-input"
$B js "document.body.textContent.includes('Success')"
```

### 7. レスポンシブなレイアウトをテストする
```bash
$B responsive /tmp/layout        # mobile + tablet + desktop screenshots
$B viewport 375x812              # or set specific viewport
$B screenshot /tmp/mobile.png
```

### 8. ファイルのアップロードをテストする
```bash
$B upload "#file-input" /path/to/file.pdf
$B is visible ".upload-success"
```

### 9. テストダイアログ
```bash
$B dialog-accept "yes"           # set up handler
$B click "#delete-button"        # trigger dialog
$B dialog                        # see what appeared
$B snapshot -D                   # verify deletion happened
```

### 10. 環境を比較する
```bash
$B diff https://staging.app.com https://prod.app.com
```

### 11. ユーザーにスクリーンショットを表示する
`$B screenshot`、`$B snapshot -a -o`、または `$B responsive` の後は、ユーザーが表示できるように、出力 PNG に対して常に読み取りツールを使用します。これがないと、スクリーンショットが表示されません。

## ユーザーハンドオフ

ヘッドレス モードで処理できない問題 (CAPTCHA、複雑な認証、多要素) に遭遇したとき
login), hand off to the user:

```bash
# 1. Open a visible Chrome at the current page
$B handoff "Stuck on CAPTCHA at login page"

# 2. Tell the user what happened (via AskUserQuestion)
#    "I've opened Chrome at the login page. Please solve the CAPTCHA
#     and let me know when you're done."

# 3. When user says "done", re-snapshot and continue
$B resume
```

**ハンドオフを使用する場合:**
- CAPTCHA またはボット検出
- 多要素認証（SMS、認証アプリ）
- ユーザーの操作が必要な OAuth フロー
- AI が 3 回試行しても処理できない複雑なインタラクション

ブラウザーは、ハンドオフ全体にわたってすべての状態 (Cookie、localStorage、タブ) を保持します。
`resume` を過ぎると、ユーザーが中断したところから新しいスナップショットが取得されます。

## スナップショット フラグ

{{SNAPSHOT_FLAGS}}

## CSS インスペクターとスタイルの変更

### 要素の CSS を検査する
```bash
$B inspect .header              # full CSS cascade for selector
$B inspect                      # latest picked element from sidebar
$B inspect --all                # include user-agent stylesheet rules
$B inspect --history            # show modification history
```

### スタイルをライブで変更する
```bash
$B style .header background-color #1a1a1a   # modify CSS property
$B style --undo                              # revert last change
$B style --undo 2                            # revert specific change
```

### きれいなスクリーンショット
```bash
$B cleanup --all                 # remove ads, cookies, sticky, social
$B cleanup --ads --cookies       # selective cleanup
$B prettyscreenshot --cleanup --scroll-to ".pricing" --width 1440 ~/Desktop/hero.png
```

## 完全なコマンドリスト

{{COMMAND_REFERENCE}}